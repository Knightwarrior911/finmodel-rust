{
  "name": "finmodel-ui-tests",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Mock-DOM regression tests for the finmodel desktop UI (Phase 4.7).",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\""
  },
  "devDependencies": {
    "jsdom": "^25.0.0"
  }
}
