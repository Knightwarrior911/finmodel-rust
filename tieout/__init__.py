# tieout — immutable filing tie-out instrument.
# This package is the MEASURING INSTRUMENT for the overnight autoresearch run.
# It is intentionally OUTSIDE the autoresearch loop scope (loop scope = src/
# extraction path only). Nothing in tieout/ may be modified by the loop, so the
# metric cannot be gamed by weakening the check.
