release: v0.9.35 - finish the house dollar rule (section-first $ + per-share cents)

Section-first $ lead on every statement section (IS/BS/CF) via a row-level
selector; all per-share/price cells now show two decimals ($#,##0.00) — IS EPS,
CF Dividend/Share, Assumptions Dividend/Share + Current Share Price, DCF
implied/current price + both sensitivity matrices, Sensitivities tables, Cover
valuation prices, Comps peer Price/52w/LTM-EPS + Implied Per-Share Price.
Aggregates ($M) and share counts stay integer; $ suppressed for non-USD.

Cell-format tests pin included/excluded rows across statements (USD+EUR) and
every valuation sheet. Gates: workspace green (fm-excel + fm-build).
