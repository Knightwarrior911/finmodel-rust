[package]
name = "fm-tieout"
version = "0.1.0"
edition = "2024"
description = "R.1 tie-out adapter: cell-by-cell scoring of model extraction vs ground truth"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde_json = "1"
