[package]
name = "fm-types"
version = "0.1.0"
edition = "2024"
description = "R.2 shared types for finmodel-core crates"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
