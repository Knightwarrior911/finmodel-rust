[package]
name = "fm-engine"
version = "0.1.0"
edition = "2024"
description = "R.3 projection engine and ledger logic"

[dependencies]
fm-types = { path = "../fm-types" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
serde_json = "1"
